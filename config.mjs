const config = {
  port: 3000,
  listen: "Listen to 3000 port",
  db: {
    connection: "mongodb://user:user@ds147544.mlab.com:47544/users",
    secret: "sfdrrkefjifji239u89fdih",
    prefix: "localhost:8080"
  }
};

export default config;